# InternFlow UI Docs

Docs are grouped by purpose:

| Folder | Content |
| --- | --- |
| `auth/` | Google OAuth setup |
| `deployment/` | Vercel deployment guides |
| `features/` | Feature-specific implementation notes |

Keep `README.md` at project root as the main entrypoint.
